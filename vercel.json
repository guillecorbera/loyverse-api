{
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "functions": {
    "api/[...path].js": {
      "runtime": "nodejs22.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
